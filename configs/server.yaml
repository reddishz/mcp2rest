server:
  port: 8080
  host: "0.0.0.0"
  mode: "websocket"  # 可选值: "websocket" 或 "stdio"

global:
  timeout: 30s
  max_request_size: "10MB"
  default_headers:
    User-Agent: "MCP2REST/1.0"
    Accept: "application/json"